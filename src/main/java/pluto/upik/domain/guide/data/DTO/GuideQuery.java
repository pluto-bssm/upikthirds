package pluto.upik.domain.guide.data.DTO;

import lombok.Data;
/**
 * 가이드 쿼리 타입을 위한 DTO 클래스
 */
@Data
public class GuideQuery {
    // 이 클래스는 GraphQL 스키마의 GuideQuery 타입을 표현하는 마커 클래스입니다.
    // 실제 필드는 없으며, 리졸버에서 타입 매핑을 위해 사용됩니다.
}